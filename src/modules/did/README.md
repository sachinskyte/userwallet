# Decentralized Identifiers (DID) Module

Use this space for DID resolution helpers, method adapters, and registry synchronization utilities. Keep integrations encapsulated to allow swapping DID methods without touching presentation logic.

