# QR & Sharing Module

Gather utilities for generating QR payloads, deep links, expiration policies, and secure share sessions. Expose composable builders that screens can invoke without knowing the underlying transport.

