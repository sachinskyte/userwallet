# Wallet Module

This directory will house wallet state management, key handling utilities, and orchestration logic for onboarding, backups, and device linking flows. Functions and hooks defined here should remain UI-agnostic so they can be reused across screens.

